# T119: Remove FragmentModel::Custom (dead code, misleading)

**Priority**: P1
**Depends on**: —
**Status**: done

## Goal

`FragmentModel::Custom` is dead code. It is never constructed or used in any
simulation path. Keeping it implies a custom fragment model is supported, which
is misleading. Remove the variant and all associated dead code.

## Steps

1. Locate `FragmentModel::Custom` and all match arms or references to it.
2. Remove the variant and any associated structs or fields.
3. `cargo fmt -- --check && cargo clippy -- -D warnings && cargo test`

## Success criteria

- [ ] `FragmentModel::Custom` is absent from the codebase.
- [ ] No dead code warnings related to this variant.
- [ ] `cargo test` passes cleanly.
