# T012: Write N_ALT_MOL and N_DUPLEX_ALT INFO fields to truth VCF

**Epic**: EPIC-OUTPUT (overallplans/EPIC-OUTPUT.md)
**Priority**: high
**Depends on**: T011
**Status**: done

## Goal

The truth VCF records `N_ALT_MOL` and `N_DUPLEX_ALT` in the INFO column for each variant, enabling callers and users to verify how many molecules supported each call.

## Steps

1. Add `N_ALT_MOL` and `N_DUPLEX_ALT` INFO header lines to the VCF writer.
2. Populate each variant record with the aggregated counts from T011.
3. Confirm fields appear correctly in output VCF.

## Notes

Completed 2026-03-23.
