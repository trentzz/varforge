# T102: Fix tumor_normal.yaml mode description

**Epic**: EPIC-EXAMPLES
**Priority**: P3
**Depends on**: none
**Status**: done

## Goal

`examples/tumor_normal.yaml` uses `samples:` (multi-sample mode) but the comment block
describes paired tumour-normal mode, which is a different code path (`paired:` config).
Correct the comment or restructure the example to use the correct mode.

## Steps

1. Read `examples/tumor_normal.yaml` to understand what it actually does
2. If it uses multi-sample mode: update the comment block to accurately describe multi-sample
   longitudinal mode, not paired mode
3. If it should be paired mode: convert it to use `paired:` config and describe paired mode
4. Add a note explaining the difference between multi-sample and paired modes
