# T141: Wire VariantTag into FASTQ output as optional config

**Epic**: EPIC-POLISH (overallplans/EPIC-POLISH.md)
**Priority**: low
**Depends on**: none
**Status**: done

## Goal

Add `annotate_reads` config option (defaults to false). When enabled,
variant-carrying reads get VT:Z: tags appended to their FASTQ names.

## Success Criteria

- [x] FASTQ read names include variant annotation when `annotate_reads: true`.
- [x] No annotation when `annotate_reads: false` (default).
- [x] Integration tests verify both states.
- [x] `cargo test` passes.
- [x] `/update` has been run after changes.
