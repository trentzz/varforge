# T034: Add `mode: paired-tumour-normal` config; define paired config schema

**Epic**: EPIC-GERMLINE (overallplans/EPIC-GERMLINE.md)
**Priority**: high
**Depends on**: none
**Status**: done

## Goal

A `mode: paired-tumour-normal` config field triggers paired simulation mode. The config schema defines separate `tumour` and `normal` output subdirectories while sharing coverage and reference settings.

## Steps

1. Add `mode` field to the top-level config with a `paired-tumour-normal` variant.
2. Define the paired config schema: shared `reference`, `coverage`, and `capture`; separate `tumour` and `normal` output subdirs.
3. Validate that both subdirs are set when paired mode is active.

## Notes

Separate `tumour/` and `normal/` output subdirs; shared coverage and reference.
