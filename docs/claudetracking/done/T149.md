# T149: Update Twist preset for kam compatibility

**Epic**: EPIC-KAM-COMPAT
**Priority**: P1
**Depends on**: T146, T147, T148
**Status**: done

## Goal

Update the `twist` preset in `src/cli/presets.rs` to produce output that
kam can consume directly, using correct Twist chemistry parameters.

## Success Criteria

- [ ] Twist preset sets `umi.inline: true`.
- [ ] Twist preset sets `umi.length: 5` (Twist default, matches kam default).
- [ ] Twist preset sets `umi.spacer: Some("AT")` (Twist 2 bp skip region).
- [ ] Twist preset sets `umi.duplex_conversion_rate: Some(0.90)`.
- [ ] Twist preset sets `umi.error_rate: Some(0.001)` (0.1% UMI error rate).
- [ ] `cargo test` passes with updated preset.
- [ ] Example config `examples/twist_duplex_benchmark.yaml` updated to reflect
      new default parameters.
