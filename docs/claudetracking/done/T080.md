# T080: Compute CCF from ClonalTree once T067 is integrated

**Epic**: EPIC-TRUTH-VCF
**Priority**: P2
**Depends on**: T067
**Status**: done

## Goal

Replace the `CCF = VAF` proxy in `src/io/truth_vcf.rs:108` with the actual cancer cell
fraction from the ClonalTree once T067 integrates it.

## Steps

1. After T067 is done, the ClonalTree exposes each clone's CCF
2. Look up the variant's clone_id in the tree to get the true CCF
3. Write `CCF=<value>` instead of `CCF=<vaf>`
4. Add a unit test: a variant in a subclone at CCF=0.3 and purity=0.6 has VAF=0.15 but
   CCF=0.3 in the truth VCF
5. `cargo test`
