# T038: Implement tandem duplicator phenotype (enriched short tandem dups 1–10 kbp)

**Epic**: EPIC-ADVANCED (overallplans/EPIC-ADVANCED.md)
**Priority**: medium
**Depends on**: none
**Status**: todo

## Goal

A tandem duplicator phenotype (TDP) SV signature generator produces an enriched rate of short tandem duplications in the 1–10 kbp range, triggered by `sv_signature: TDP`.

## Steps

1. Implement a `TdpSvGenerator` that samples tandem duplication positions and sizes from the TDP-typical distribution (1–10 kbp).
2. Integrate with the existing SV spike-in path.
3. Accept `sv_signature: TDP` in the mutations config block.

## Notes

`sv_signature: TDP`.
