# T018: Add integration test: preset fills expected defaults

**Epic**: EPIC-PRESETS (overallplans/EPIC-PRESETS.md)
**Priority**: high
**Depends on**: T017
**Status**: done

## Goal

An integration test loads a minimal YAML config with only a `preset` key set and asserts that the resulting `Config` struct contains the expected preset defaults.

## Steps

1. Write a test config YAML with `preset: illumina-umi` and no other fields.
2. Load and deserialise the config.
3. Assert that preset-specific fields (e.g. UMI length, read length) match the expected defaults.

## Notes

Completed 2026-03-23.
