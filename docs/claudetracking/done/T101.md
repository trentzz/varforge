# T101: Fix misleading VCF compression comment in custom_mutations.yaml

**Epic**: EPIC-EXAMPLES
**Priority**: P3
**Depends on**: none
**Status**: done

## Goal

The comment in `examples/custom_mutations.yaml` states the VCF "must be bgzip-compressed and
tabix-indexed" but `src/io/vcf_input.rs` accepts plain-text VCF. Correct the comment.

## Steps

1. Read `examples/custom_mutations.yaml` and locate the comment
2. Replace with: "VCF may be plain-text (.vcf) or bgzip-compressed (.vcf.gz). A .tbi index
   is required for .vcf.gz files."
3. Verify the claim is accurate by checking vcf_input.rs
