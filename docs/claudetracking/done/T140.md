# T140: Extract sub-functions from generate_region

**Epic**: EPIC-POLISH (overallplans/EPIC-POLISH.md)
**Priority**: low
**Depends on**: none
**Status**: done

## Goal

`SimulationEngine::generate_region` is approximately 500 lines long and handles
fragment sampling, variant spike-in, quality/error injection, UMI family
expansion, SV processing, and artifact injection. Extract logical blocks into
named private methods to improve readability and testability.

## Success Criteria

- [ ] `generate_region` body is under 200 lines.
- [ ] At least four helper methods are extracted (e.g. `apply_small_variants`, `apply_sv_variants`, `expand_umi_families`, `build_read_pair`).
- [ ] All existing tests pass without modification.
- [ ] No change in simulation output for a given seed (determinism preserved).
- [ ] `/update` has been run after changes.

## Steps

1. Identify logical blocks within `generate_region`.
2. Extract each into a private method on `SimulationEngine`.
3. Verify determinism by running the test suite and comparing output checksums.

## Notes

This is a pure refactoring task. No behaviour change.
