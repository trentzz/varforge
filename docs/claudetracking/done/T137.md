# T137: Add missing module doc comments

**Epic**: EPIC-POLISH (overallplans/EPIC-POLISH.md)
**Priority**: medium
**Depends on**: none
**Status**: todo

## Goal

Nine source files lack module-level `//!` doc comments. Add a one to three
line doc comment to each.

## Success Criteria

- [ ] Every `.rs` file in `src/` begins with a `//!` module doc comment.
- [ ] Specifically: `main.rs`, `cli/mod.rs`, `cli/simulate.rs`, `cli/validate.rs`, `core/gc_bias.rs`, `io/bam.rs`, `io/truth_vcf.rs`, `tumour/clonal_tree.rs`, `variants/structural.rs`.
- [ ] `cargo doc --no-deps` succeeds.
- [ ] `/update` has been run after changes.

## Steps

1. Add doc comments to each of the nine files listed above.
2. Run `cargo doc --no-deps` to verify.

## Notes

Keep comments concise. One sentence describing what the module does.
