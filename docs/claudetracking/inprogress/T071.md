# T071: Fix R2 mate position calculation in bam.rs

**Epic**: EPIC-BAM-FIDELITY
**Priority**: P1
**Depends on**: none
**Status**: todo

## Goal

Fix `src/io/bam.rs:135` where `pos2 = pos + r1_ref_span + 1` places R2 immediately after R1
rather than at the correct position within the fragment. The correct formula is
`pos + fragment_length - r2_read_length` (for a non-overlapping pair where fragment = R1 + gap + R2).

## Steps

1. Read the current pos2 calculation and understand the fragment model
2. Determine the correct formula: `pos2 = fragment_start + fragment_length - r2_len`
3. Update both `write_pair` and `write_pair_with_umi`
4. Update PNEXT (mate position) in both R1 and R2 records
5. Add a unit test: write a pair with fragment_length=300, read_length=150, verify pos2 == pos + 150
6. `cargo test`
