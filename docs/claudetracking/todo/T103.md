# T103: Audit all dead_code annotations; remove unused items

**Epic**: EPIC-DEAD-CODE
**Priority**: P2
**Depends on**: none
**Status**: todo

## Goal

Reduce the 67 `#[allow(dead_code)]` annotations to only those covering planned future work.
Remove genuinely unused items. Annotate kept items with the task ID they are waiting for.

## Steps

1. Grep for all `#[allow(dead_code)]` across src/
2. For each: check whether the item is referenced anywhere in non-test code
3. If truly unused and not tied to a planned task: delete it
4. If tied to a planned task (e.g. ClonalTree → T067): replace the annotation with a comment
   `// TODO(T067): remove allow once integrated`
5. Prioritise: `src/variants/structural.rs` (7), `src/core/types.rs` (4),
   `src/umi/barcode.rs` (3), `src/core/profile_learner.rs` (3)
6. `cargo clippy -- -D warnings && cargo test`
