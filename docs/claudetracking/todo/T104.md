# T104: Remove PcrFamilySizeSampler if no task plans to use it

**Epic**: EPIC-DEAD-CODE
**Priority**: P3
**Depends on**: T103
**Status**: todo

## Goal

`src/core/fragment.rs` contains `PcrFamilySizeSampler` marked `#[allow(dead_code)]`. If no
planned task (T001–T104) requires it, remove it.

## Steps

1. Search all task files and code comments for any reference to PcrFamilySizeSampler
2. If no task requires it: delete the struct and its impl from fragment.rs
3. If a future task might use it: add a `// TODO(T??): keep for family size simulation`
   comment instead
4. `cargo clippy -- -D warnings && cargo test`
