# T058: Remove noodles_base_to_u8 dead wrapper

**Epic**: EPIC-REFACTOR
**Priority**: P3
**Depends on**: none
**Status**: todo

## Goal

Delete `pub fn noodles_base_to_u8(b: &u8) -> u8 { *b }` from `src/editor/read_modifier.rs`
and inline the dereference at all call sites (or remove the call sites if the function is
unused).

## Steps

1. Grep for all uses of `noodles_base_to_u8` across the codebase
2. If unused: delete the function and any `#[allow(dead_code)]` attribute above it
3. If used: replace each call with `*b` or the appropriate deref
4. `cargo clippy -- -D warnings` to confirm no warnings remain
